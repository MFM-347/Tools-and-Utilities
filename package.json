{
  "name": "tools-utilities",
  "scripts": {
    "format": "prettier -2 ./",
    "dev": "python3 -m http.server 8000",
    "cmd": "sh -c 'find . -type f -name \"*.sh\" -exec chmod +x {} \\;'",
    "start": "clear && npm run format && npm run dev"
  }
}